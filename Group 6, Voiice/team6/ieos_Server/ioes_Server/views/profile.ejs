<!doctype html>
<html>
  <head>
    <title>Smart Home</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
      <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
        <style>
          body         { padding-top:80px; word-wrap:break-word; }
        </style>
      </head>
  <body>
    <div class="container">
  
      <div class="page-header text-center">
        <h1>
          <span class="fa fa-bullseye"></span> Devices list
        </h1>
        <a href="/logout" class="btn btn-default btn-sm">Logout</a>
        <% if (user.local.permission == "admin"){%>
        <span class="fa fa-user-plus"></span>
        <a href="/edit"class ="btn btn-default btn-sm">Edit</a>
        <% } %>
      </div>
      <table  class ="table table-hover">
        <thead>
          <tr>
            <td>User</td>
            <% for (var i in devices_list){%>
            <td>
              <%= devices_list[i].device_name%>

            </td>
            <td>
              <select class="form-control input-sm">
                <% for(var j =0;j< devices_list[i].actions.length;j++){ %>
                <option>
                  <%= devices_list[i].actions[j] %>
                </option>
                <% } %>
              </select>
            </td>
            <% }%>
          </tr>
        </thead>
        <tbody>

          <tr>
            <td>
              <%= user.local.name%>
            </td>
            <% for (var i in devices_list) { %>
            <% if(user.local.allow_devices.indexOf(devices_list[i].device_name)==-1){ %>
            <td>
              <span class="fa fa-times"></span>
            </td>
            <% }else{ %>

            <td>
              <span class="fa fa-circle-o"></span>
              </td>
             <% } %>
               <td></td>
            <% } %>
          </tr>

        </tbody>

      </table>

    </div>
  </body>
</html>
